import { LoadingCont } from "./Loading.styled";

const LoadingPage = () => {
    return (
        <LoadingCont>
            <div class="loader">Loading...</div>
        </LoadingCont>
    );
};

export default LoadingPage;